# SEO Analytics Dashboard (Next.js + Tailwind)

Minimal, deploy-ready project for Vercel.

## Run locally
```bash
npm install
npm run dev
```

## Deploy
Push the repo to GitHub and import to Vercel. Framework: Next.js (auto). Build: `next build`.
